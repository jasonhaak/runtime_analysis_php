<?php
ini_set("memory_limit", "10240M");

/**
 * Example Function
 * 
 * Functions:
 * - foo($size): Performs a multipliction multiplication for two numbers of the size $size.
 *   @param int $size: The number
 *   @return int The result of multiplication
 *
 */

// Example function
function foo($size) {
    sleep(1);
    return $size * $size; // Return result
}
?>
